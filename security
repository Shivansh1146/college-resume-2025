subprocess.run(['program', 'arg1', 'arg2'], capture_output=True, text=True, check=True)

validated_input = input('Enter command: '); validated_input = shlex.quote(validated_input); subprocess.run(['ls', '-l'], capture_output=True, text=True, check=True)
validated_input = input('Enter command: '); validated_input = shlex.quote(validated_input); subprocess.run(['ls', '-l'], capture_output=True, text=True, check=True)
validated_input = input('Enter command: '); validated_input = shlex.quote(validated_input); validated_input = shlex.split(validated_input); subprocess.run(validated_input, capture_output=True, text=True, check=True)
import subprocess
validated_input = input('Enter command: '); validated_input = shlex.quote(validated_input); subprocess.run(['ls', '-l'], capture_output=True, text=True, check=True)
validated_input = input('Enter command: '); validated_input = shlex.quote(validated_input); subprocess.run(['ls', '-l'], capture_output=True, text=True, check=True)
validated_input = input('Enter command: '); validated_input = shlex.quote(validated_input); validated_input = shlex.split(validated_input); subprocess.run(validated_input, capture_output=True, text=True, check=True)
validated_input = input('Enter command: '); validated_input = shlex.quote(validated_input); subprocess.run(['ls', '-l'], capture_output=True, text=True, check=True)
validated_input = input('Enter command: '); validated_input = shlex.quote(validated_input); subprocess.run(['ls', '-l'], capture_output=True, text=True, check=True)
user_input = input("Enter command: ")
validated_input = shlex.quote(user_input)
subprocess.run(shlex.split(validated_input), capture_output=True, text=True, check=True)
